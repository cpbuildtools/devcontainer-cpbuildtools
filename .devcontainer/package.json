{
  "name": "cp-build-tools-dev-environment",
  "scripts": {
    "postCreate": "ws install -p && ws run postCreate",
    "onCreate": "ws run onCreate",
    "postAttach": "ws run postAttach",
    "postStart": "echo postStart"
  },
  "workspaces": [
    "../projects/*"
  ]
}
